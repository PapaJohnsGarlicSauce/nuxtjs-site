{
  "name": "nuxtjs-site",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.12.1"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
